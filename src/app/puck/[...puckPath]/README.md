# Edit Mode (Using Puck Editor)

The dynamically generated path which injects Puck Editor at the following url format:

```
/procs/{PROC-NAME}/edit
```

To enable the puck editor across other locations of the app modify the [/src/middleware.ts](/src/middleware.ts) file
